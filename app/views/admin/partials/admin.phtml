<?php var_dump($data); ?>
<main>
 
        <h1>Bienvenue sur ton site Mathieu </h1>

        <h2>Gérer la Boutique</h2>
        

       <table>
        <thead>
             <tr>
                <th>Titre</th>
                <th>Image</th>
                <th>Description</th>
                <th>Prix</th>
                <th>suprimer</th>
                <th>Mettre a jour</th>
             </tr>
       </thead>
        <tbody>
          
            <?php foreach ($data['books'] as $key => $book) : ?>
                <tr>
                    <td id="nameBook"><?= $book->getName() ?></td>
                    <td id="imgBook"><?= $book->getimg() ?></td>
                    <td id="resumeBook"><?= $book->getResume() ?></td>
                    <td id="priceBook"><?= $book->getPrice() ?> €</td>
                    <td>
                        <form method="post">
                            <input type="hidden" value="<?= $book->getId() ?>" name="bookid"/>
                            <input type="submit" name="deleteBook" value="Supprimer">
                        </form>
                    </td>
                    <td><a href="<?= $base_uri[0] ?>/public/admin/update">Mettre à jour</a></td>
                </tr>
            <?php endforeach; ?>
       </tbody>
      </table>

    </main>
